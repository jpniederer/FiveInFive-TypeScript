// Variables
let x: number = 42;
const y: number = 2;

// Arrays
let words: string[] = ["hi", "hello", "howdy"];

// Generics
let genericNumbers: Array<number> = [5, 10, 15];

// Tuple
let t: [string, number];
t = ["key", 6];

// Enum
enum Grades { 
  A, B, C, D, F
}